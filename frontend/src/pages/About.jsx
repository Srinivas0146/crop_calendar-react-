import React from "react";

export default function About() {
  return (
    <section className="container">
      <h2>About</h2>
      <p className="mt1">
        CropWise geocodes any Indian state/UT/city you type, fetches forecast (temp & rainfall),
        determines the likely season dynamically, and evaluates crops against admin-editable rules.
      </p>
    </section>
  );
}
